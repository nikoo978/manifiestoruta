Ruta Envios v2.5.4 - actualización limpia del repositorio

1. Descomprimí este ZIP.
2. Copiá TODO su contenido a la raíz del repositorio (donde está .git), reemplazando archivos.
3. Si existe una carpeta anidada vieja llamada manifiestoruta, eliminála.
4. Ejecutá:

  git status
  git add -A
  git commit -m "feat: lazy map, source picker and deeper OCR v2.5.4"
  git push origin main

Vercel debe compilar desde la raíz y mostrar v2.5.4 / Service Worker v25.
